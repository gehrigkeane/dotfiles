##############################################################################
# Shell-agnostic env (.profile) is sourced once from ~/.zshrc — not here — to
# avoid double-sourcing (which duplicated PATH entries). Login shells still get
# it because ~/.zshrc runs after ~/.zprofile.
##############################################################################

##############################################################################
# History Configuration
# Note: `head ~/.zsh_history` -> : 1671052122:0;xh --verify no "https://prod-offer-service-1671050729.us-east-1.elasticbeanstalk.com/metrics/service.http.client.UNKNOWN.uri._user_userId_redeem"
##############################################################################
# moved to ~/.zshrc

# Homebrew M1 artifact
eval "$(brew shellenv)"

# mise: interactive activation lives in ~/.zshrc (`mise activate zsh`). Do not
# also load shims here — running both double-munges PATH.

# Added by OrbStack: command-line tools and integration
# Comment this line if you don't want it to be added again.
source ~/.orbstack/shell/init.zsh 2>/dev/null || :

# Added by Obsidian
export PATH="$PATH:/Applications/Obsidian.app/Contents/MacOS"

